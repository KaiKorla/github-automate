name: Sync microsoft/winget-pkgs

on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - run: gh repo sync microsoft/winget-pkgs -b master
        env:
          GH_TOKEN: ${{ secrets.SYNC_TOKEN }}
